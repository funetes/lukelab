import RecorderApp from "../components/recorder";

const Recorder = () => {
  return (
    <main className="bg-gray-700 flex items-start justify-center h-screen">
      <RecorderApp />
    </main>
  );
};

export default Recorder;
